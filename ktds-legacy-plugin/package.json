{
  "name": "ktds-legacy-plugin",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "description": "ktds 레거시 문서 자동화 플러그인 (DS-NAVI) — U-A fork sibling workspace",
  "scripts": {
    "build": "pnpm --filter @ktds/legacy-core build",
    "test": "pnpm --filter @ktds/legacy-core test"
  }
}
