// STUB — owned by the `config` workstream (plan 단계1.3).
// Implement: understanding.config.json zod schema + loader + .spec scaffold.
// Defaults: networkType 3, outputLanguage "ko", inferredRatioWarnThreshold 0.3,
//           inferredRatioBlockThreshold 0.6, supportedSchemaVersions ["1.0.0"].
export {};
