{
  "description": "Standalone JSP page routing. Every *.jsp file under the webapp root is a directly addressable GET/page/jsp route whose path is the file path relative to the webapp root. JSPs under WEB-INF are forward-only (internal) and are excluded.",
  "routes": [
    { "method": "GET", "path": "/account/profile.jsp", "kind": "page", "framework": "jsp", "handler": null, "notes": [] },
    { "method": "GET", "path": "/index.jsp", "kind": "page", "framework": "jsp", "handler": null, "notes": [] }
  ],
  "batchEntries": []
}
