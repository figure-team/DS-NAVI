# 프로젝트 AI 문서 자동화 — MVP 발표 자료

> 슬라이드 단위로 구성. `---` 가 한 장의 구분선입니다.

---

## 1. 개요

> ### "기존 프로젝트에 명령 한 번이면, **코드 근거가 붙은 운영 문서**가 자동으로 만들어집니다."

Understand-Anything(오픈소스) 기반 **Claude Code 플러그인**. 별도 프로그램 설치 없이, 쓰던 AI CLI 명령으로 바로 사용합니다.

> 📌 이번 **MVP는 "근거 기반 문서 자동화 + 검토/승인"** 을 증명하는 단계입니다.

---

## 2. 문제 (Before)

기존 프로젝트의 유지보수에서 반복되는 고통:

- 📄 **문서와 코드가 따로 논다** — 마지막 업데이트가 언제인지 모른다.
- 🧑‍💻 **담당자가 떠나면 깜깜이** — 시스템 구조가 사람 머릿속에만 있다.
- 🗄️ **PL/SQL·MyBatis는 미궁** — 화면-API-테이블 연결을 손으로 추적한다.
- 🆕 **온보딩에 수주** — 신규 투입 시 서비스 파악에 오래 걸린다.

---

## 3. 기대 효과 (After)

| 항목             | Before          | After                          | 단계 |
| ---------------- | --------------- | ------------------------------ | :--: |
| 신규 투입 온보딩 | 수주            | **1~2일**                      | MVP  |
| 문서 최신화      | 수동, 누락 잦음 | **실행할 때마다 자동 갱신**    | MVP  |
| PL/SQL 파악      | 담당자 의존     | LLM 요약 + `[추정]` 태그       | MVP  |
| 문서 신뢰성      | 보증 없음       | 검토·승인·감사 내재화          | MVP  |
| 변경 영향도 파악 | 수동 코드 추적  | `/understand-impact` 자동 분석 | MVP+ |

---

## 4. 핵심 가치 2가지

### ① 근거 기반 분석

모든 문장에 `근거: 파일경로:라인` 이 붙습니다. 근거가 없으면 **저장 자체가 차단**됩니다.

> 예) `LoginController는 /login 요청을 처리한다. [확정(AI)] 근거: LoginController.java:42`

### ② 검토·승인·감사

AI가 쓴 초안(DRAFT)을 담당자가 검토·승인(APPROVED)하고, 모든 과정이 감사 로그로 남습니다.

---

## 5. 시스템 흐름

```
[프로젝트 코드]
   │  /understand-docs
   ▼
┌─────────────────────────────────────────────┐
│ AI 분석   지식 그래프 → 근거 매핑 → 문서화    │
│   코드 근거가 붙은 문장만 통과                │
└───────────────┬─────────────────────────────┘
                ▼
[근거 붙은 문서 5종]  →  담당자 검토·승인  →  APPROVED
```

> 각 문장엔 **근거**가, 각 단계엔 **감사 로그**가 자동으로 따라붙습니다.

---

## 6. 유스케이스 ① — 최초 분석

**상황**: 프로젝트 PM이 (인수받은/유지보수 중인) 프로젝트를 **처음으로** 분석해 기준 문서 세트를 만든다.

```
$ /understand-init          # 프로젝트 초기화 (VCS 자동 감지, 설정 생성)
$ /understand               # 코드베이스 스캔 → 지식 그래프 생성
$ /understand-docs          # 근거 기반 5종 문서 자동 생성
```

**결과 — `docs/` 폴더에 즉시 생성**

| 문서                 | 보는 것                                    |
| -------------------- | ------------------------------------------ |
| `01_tech-stack.md`   | 어떤 언어·프레임워크·라이브러리를 쓰나     |
| `02_architecture.md` | 레이어 구조와 모듈 의존 관계               |
| `03_feature-spec.md` | 어떤 업무 기능이 있고 어디서 시작되나      |
| `04_api-spec.md`     | 어떤 API가 있고 어느 Controller가 처리하나 |
| `05_db-spec.md`      | 어떤 테이블·컬럼이 있고 어느 Mapper가 쓰나 |

> PM이 프로젝트 전체 구조를 **코드 근거와 함께 한 번에** 확보합니다. 이후 투입되는 팀원은 이 문서로 빠르게 온보딩하고, 실행할 때마다 문서가 자동 갱신됩니다.

---

## 7. 유스케이스 ② — 문서 신뢰성 (검토·승인)

**상황**: AI가 쓴 문서를 공식 운영 문서로 채택하기 전 검증이 필요하다.

```
$ /understand-docs review --list                      # 검토 대기 + [추정] 항목 수
$ /understand-docs review --doc 04_api-spec.md         # [추정] → [확정(홍길동)] 확정
$ /understand-docs approve --doc 04_api-spec.md --by "홍길동"   # 공식 승인
```

- `[확정(AI)]` / `[확정(담당자)]` / `[추정]` / `[확인 필요]` 4단계 신뢰도 태그
- 동적 SQL·리플렉션 등 자동 판단이 어려운 항목은 `[확인 필요]`로 남겨 **담당자에게 위임**
- 생성·검토·승인 전 과정이 감사 로그에 기록

---

## 8. 유스케이스 ③ — 변경 영향도 분석 〔MVP+〕

**상황**: "로그인에 간편 로그인을 추가하면 어디까지 영향이 갈까?"

```
$ /understand-impact 로그인 기능에 간편 로그인 추가        # 자연어
$ /understand-impact --path src/auth/LoginService.java   # 파일 지정
```

**분석 범위**: API 영향 / DB 영향 / 업무 흐름 영향 / 연관 모듈
**출력**: `docs/09_release/change-impact-analysis.md`

> 수정 **전·후** 모두 영향 범위를 미리 보여줍니다.

---

## 9. 데모 시나리오 (MVP 전체 흐름)

```
1. /understand-init        → 프로젝트 인식, 설정 생성
2. /understand             → 지식 그래프 생성 (U-A)
3. /understand-docs        → 근거 붙은 5종 문서 (DRAFT)
4. /understand-docs review → [추정] 항목 검토·확정
5. /understand-docs approve → 공식 승인 (APPROVED)
6. /understand-export      → HTML 산출물
```

각 문서 문장마다 **근거**가, 각 단계마다 **감사 로그**가 자동으로 따라붙습니다.

---

## 10. 단계별 구현 범위

**✅ 이번 MVP (개방형 — Claude Code 기준, 비민감 샘플)**

- `/understand-init`, `/understand-docs`, `/understand-export`(HTML)
- 근거 붙은 5종 문서 생성
- DRAFT → 검토 → 승인 → 감사 로그

**🔜 MVP+ (안정화 후)**

- `/understand-impact` 변경 영향도, `/understand-history` 이력
- `/understand-custom-dashboard`, `/understand-export` PPT/Word 추가 포맷

---

## 11. WBS (작업 분해 · 일정)

> 주말 제외. Discovery(선행) 후 MVP 본작업은 **43영업일**.

| WBS | 작업 패키지          | 주요 산출물                                                          | 기간(2026)  | 영업일 |
| :-: | -------------------- | -------------------------------------------------------------------- | ----------- | :----: |
|  0  | Discovery (선행)     | U-A baseline·플러그인 설치·**비민감 샘플 fixture 2종**               | 05-25~06-05 |   10   |
|  1  | Core 확장 골격       | kg-reader 변환기, canonical model, evidence mapper IF, config schema | 06-08~06-19 |   10   |
|  2  | 문서 생성            | spec-generator, 5종 템플릿, 근거(claim/evidence) validator, 청킹     | 06-22~07-03 |   10   |
|  3  | 검토/승인/감사       | DocStateMachine, ApprovalWorkflow(review/approve), AuditLogger       | 07-06~07-17 |   10   |
|  4  | Export/안정화        | HTML exporter, 성능 측정, CLI smoke(Codex/Gemini/OpenCode)           | 07-20~07-28 |   7    |
|  5  | 매뉴얼/검토·릴리즈   | 설치·운영·장애 대응 가이드, 릴리즈 게이트 점검                       | 07-29~08-05 |   6    |
|  6  | **MVP+** (안정화 후) | `/understand-impact`·`/understand-history`·대시보드·PPT/Word         | 별도        |   —    |

**일정 한눈에 (주 단위)**

```
            W1   W2   W3   W4   W5   W6   W7   W8
            6/08 6/15 6/22 6/29 7/06 7/13 7/20 7/29
1 Core      ████ ████
2 문서생성             ████ ████
3 검토/승인/감사                  ████ ████
4 Export/안정화                            ████
5 매뉴얼/릴리즈                                  ████
```

> 일정 리스크 시 **WBS 6(MVP+)부터 축소**. WBS 2·3(근거 스키마·검토/승인)은 필수라 축소 대상이 아님.

---

## 12. 지원 환경 (MVP)

| 환경     | 지원                                                            |
| -------- | --------------------------------------------------------------- |
| 네트워크 | 개방형(Claude Code 기준선)                                      |
| 버전관리 | Git · SVN · VCS 없음(스냅샷 해시) 모두 지원                     |
| 개발     | Java/Spring · MyBatis XML · Oracle SQL/PL-SQL · YAML/Properties |
| 설치     | 온라인 plugin 마켓플레이스 설치 + 폐쇄망 fork clone 오프라인 설치 |

---

## 13. 목표 지표 (MVP 합격선)

| 무엇을 | 목표                                                 |
| ------ | ---------------------------------------------------- |
| 근거율 | `[확정(AI)]` 문장 95% 이상 코드 근거 포함            |
| 속도   | 50K LOC 기준 초기 분석~문서 저장 **5분 이내**(PoC)   |
| 신뢰성 | 생성→검토→승인 흐름과 감사 로그 재현                 |
| 샘플   | 비민감 Java/Spring/MyBatis/Oracle 샘플 2종 이상 검증 |

---

## 14. 정리

- 기존 프로젝트에 **근거 기반 문서 + 검토/승인**을 입히는 자동화 도구입니다.
- 별도 CLI 없이 쓰던 AI CLI 명령으로, 오픈소스를 최대한 보존하며 확장합니다.
- **MVP**: 비민감 샘플에서 **①근거 + ②신뢰성** 두 축을 증명합니다.
