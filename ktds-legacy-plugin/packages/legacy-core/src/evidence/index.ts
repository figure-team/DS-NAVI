// STUB — owned by the `evidence` workstream (plan 단계1.5).
// Implement: claim/confidence/evidence mapper + validator
//   (CONFIRMED_AI without evidence -> RETURNED, A5) + computeInferredRatio()
//   per-doc (A3/A6 denominator).
export {};
