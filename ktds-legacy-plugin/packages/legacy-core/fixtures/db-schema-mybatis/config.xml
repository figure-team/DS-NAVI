<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!-- 비매퍼 XML — 역추론 대상이 아님(루트 요소 판별). FROM FAKE_TABLE 언급도 무시돼야 한다. -->
  <settings>
    <setting name="query" value="SELECT 1 FROM FAKE_TABLE"/>
  </settings>
</configuration>
