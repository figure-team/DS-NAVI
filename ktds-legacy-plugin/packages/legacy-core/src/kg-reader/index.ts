// STUB — owned by the `kg-reader` workstream (plan 단계1.4).
// Implement: U-A knowledge-graph.json (GraphNode/GraphEdge) -> CanonicalGraph,
//   uid derivation ({container}#{name}, lineRange[0] suffix on collision),
//   version guard (["1.0.0"]) + structural fingerprint guard (A14).
// Read the on-disk JSON contract only; do NOT import @understand-anything/core.
export {};
